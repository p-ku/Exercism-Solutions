(import (rnrs))

(define (double x)
  (* 2 x))
(define (halve x)
  (/ x 2))

(define (mul a b)
  (if (> a b)
    (cond ((= 0 b) 0)
          ((even? n) (mul 
          (else (+ a (mul (- b 1)))))
    
